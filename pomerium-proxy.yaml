# Patch para el Service pomerium-proxy (creado por el operador de Pomerium).
# Aplicar con:
#   kubectl patch service pomerium-proxy -n pomerium --type merge --patch-file pomerium-proxy.yaml
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: 'authenticate.prueba.ticksar.com.ar'
