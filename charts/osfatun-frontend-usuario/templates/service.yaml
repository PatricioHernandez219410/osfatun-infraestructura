apiVersion: v1
kind: Service
metadata:
  name: frontend-usuario
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "osfatun-frontend-usuario.labels" . | nindent 4 }}
spec:
  selector:
    app: osfatun-frontend-usuario
  ports:
    - name: http
      port: 80
      targetPort: 80
