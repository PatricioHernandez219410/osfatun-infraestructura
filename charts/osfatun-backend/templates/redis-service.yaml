apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "osfatun-backend.labels" . | nindent 4 }}
spec:
  selector:
    app: osfatun-redis
  ports:
    - port: 6379
      targetPort: 6379
