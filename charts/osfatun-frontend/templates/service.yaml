apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "osfatun-frontend.labels" . | nindent 4 }}
spec:
  selector:
    app: osfatun-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
